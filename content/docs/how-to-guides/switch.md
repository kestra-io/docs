---
title: Switch
---

Depending on the value passed as the input, the flow will branch to different tasks. If there is no matching value, the `defaults` task is used.

```yaml file=public/examples/flows_switch.yml
```
