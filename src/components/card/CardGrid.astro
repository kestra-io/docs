---
import { Icon } from "astro-icon/components"

interface Item {
    icon?: string;
    title: string;
    subtitle?: string;
    description?: string;
    link?: {
        href: string;
        text: string;
    };
    evenBg?: boolean;
}

interface Props {
    title: string;
    subtitle?: string;
    items: Item[];
    align?: "left" | "center";
}

const { title, subtitle, items, align = "center" } = Astro.props;
---

<section class="card-grid-section">
    <div class="container">
        <div class:list={["head", { "align-left": align === "left" }]}>
            <h2 data-usal="fade-r">
                <Fragment set:html={title} />
                <slot />
            </h2>
            <p set:html={subtitle} data-usal="fade-l" />
        </div>

        <div class="cards-grid">
            {
                items.map((item, index) => (
                    <div
                        class:list={["card", { "even-bg": item.evenBg }]}
                        data-usal={`${index % 2 === 0 ? "fade" : "zoomin"} delay-0 duration-400 ease-out`}
                    >
                        {item.icon && (
                            <Icon name={item.icon} class="icon" />
                        )}
                        <h6 class="title" set:html={item.title} data-usal="fade-l"/>
                        {item.link ? (
                            <a href={item.link.href} class="btn btn-primary">
                                {item.link.text}
                            </a>
                        ) : (
                            item.description && <p class="description" set:html={item.description} data-usal="fade-l" />
                        )}
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style lang="scss">
    @import "~/assets/styles/variable";

    .card-grid-section {
        background-color: var(--ks-background-body);
        width: 100%;
        padding: 3rem 0.5rem;

        @include media-breakpoint-up(md) {
            padding: 4rem 8px;
            padding-bottom: 90px;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 2rem;

            h2 {
                text-align: center;
                color: var(--ks-content-primary);
            }

            .head {
                width: 100%;
                display: flex;
                flex-direction: column;
                align-items: center;

                &.align-left {
                    align-items: flex-start;

                    h2, p {
                        text-align: left;
                    }

                    h2 {
                        justify-content: flex-start;
                    }
                }
            }

            .cards-grid {
                display: flex;
                flex-wrap: wrap;
                gap: 1rem;
                justify-content: center;
                width: 100%;

                .card {
                    border-radius: 12px;
                    background: var(--ks-background-primary);
                    padding: 2rem;
                    display: flex;
                    flex-direction: column;
                    justify-content: flex-start;
                    align-items: flex-start;
                    gap: 8px;
                    border: 1px solid var(--ks-border-secondary);
                    box-shadow: 2px 3px 16px 0px var(--ks-shadows-light);

                    @include media-breakpoint-up(lg) {
                        flex: 0 0 calc(33.333% - 1rem);
                        max-width: 360px;
                    }

                    @include media-breakpoint-down(lg) {
                        flex: 0 0 calc(50% - 1rem);
                    }

                    @include media-breakpoint-down(md) {
                        flex: 0 0 100%;
                    }

                    .icon {
                        font-size: 1.5rem;
                        align-self: flex-start;
                        color: var(--ks-content-link);
                        width: 1.5rem;
                        height: 1.5rem;
                    }

                    .title {
                        font-weight: 700;
                        line-height: 28px;
                        margin: 0;
                        color: var(--ks-content-primary);
                    }

                    .description {
                        font-weight: 400;
                        font-size: $font-size-sm;
                        line-height: 22px;
                        margin: 0;
                        color: var(--ks-content-primary);

                        :global(a) {
                            color: var(--ks-content-link) !important;
                        }
                    }

                    &.even-bg {
                        background: var(--ks-background-secondary) !important;
                        justify-content: center;
                        align-items: center;
                        text-align: center;
                        gap: 0.5rem;
                    }

                    .btn {
                        padding: 0.5rem 1.5rem;
                        font-size: $font-size-base;
                        font-weight: 700;
                        border-radius: 8px;
                    }
                }
            }
        }
    }
</style>
