---
import { marked } from "marked"

const props = Astro.props as {
    content: string
}

const { content, ...rest } = props

const htmlContent = await marked.parse(content)
---

<div set:html={htmlContent} {...rest} />
