name: Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-deploy
  cancel-in-progress: false

permissions:
  pull-requests: write

jobs:
  truncate-url-slug:
    name: Truncate URL
    runs-on: ubuntu-latest
    outputs:
      slug: ${{ steps.preview-url.outputs.slug }}
      url: ${{ steps.preview-url.outputs.url }}
    steps:
      # Only the composite action is needed here, so skip the full tree.
      - name: Checkout
        uses: actions/checkout@v6
        with:
          sparse-checkout: .github/actions

      - name: Resolve preview URL
        id: preview-url
        uses: ./.github/actions/preview-url

  deploy-workers:
    name: Publish to Cloudflare Workers
    runs-on: ubuntu-latest
    needs: truncate-url-slug
    env:
      CF_PAGES_BRANCH: ${{ github.ref_name }}
      CF_PAGES_COMMIT_SHA: ${{ github.sha }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      # Preview and production render different image URLs and gated content,
      # and Astro's own config hash does not cover either, so they cache apart.
      BUILD_VARIANT: ${{ startsWith(github.repository, 'kestra-io/') && github.ref != 'refs/heads/main' && 'preview' || 'production' }}

    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'Production' || 'Preview' }}
      url: ${{ needs.truncate-url-slug.outputs.url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node via nvm
        uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"

      - name: Install dependencies
        run: npm install

      - name: Check usage of collection in SSR pages
        if: ${{ startsWith(github.repository, 'kestra-io/') && github.event_name == 'pull_request'}}
        run: node scripts/check-astro-content-imports.mjs

      # Holds the content store plus the incremental build manifest and its copy
      # of the pages from the last build, which is what lets pages be skipped.
      - name: Restore Astro build cache
        uses: actions/cache@v4
        with:
          path: node_modules/.astro
          key: astro-build-${{ env.BUILD_VARIANT }}-${{ github.ref_name }}-${{ github.sha }}
          # Only the same ref and variant: main builds production, PR builds
          # preview, so a cross-ref fallback would never match anything.
          restore-keys: astro-build-${{ env.BUILD_VARIANT }}-${{ github.ref_name }}-

      # TEMPORARY (test branch only): tee the build and stash a profile, printed
      # by the last step of the job so the log tail can actually reach it.
      - name: Build
        run: |
          set -o pipefail
          npm run build 2>&1 | tee /tmp/build.log
          d=dist; [ -d dist/client ] && d=dist/client
          # Profiling only from here: steps run under `bash -e`, and a
          # SIGPIPE from `| head` would abort a build that actually passed.
          set +e +o pipefail
          {
            echo "--- first lines (content sync, loaders) ---"
            head -12 /tmp/build.log
            echo "--- phase lines ---"
            grep -aE "\[(build|vite|content|types|adapter|glob-loader)\]" /tmp/build.log | tail -40
            echo "--- cachekey probe ---"
            grep -a "cachekey-probe" /tmp/build.log | sort -u
            echo "--- route buckets ---"
            perl -pe 's/\e\[[0-9;]*m//g' /tmp/build.log > /tmp/build.plain
            grep -a -e '├─' -e '└─' /tmp/build.plain > /tmp/pages.txt
            echo "page lines: $(wc -l < /tmp/pages.txt)"
            echo "sample restored:"
            grep -a '(restored)' /tmp/pages.txt | head -3
            echo "sample not restored:"
            grep -av '(restored)' /tmp/pages.txt | head -3
            seg() { sed -n 's#^[^/]*\(/[^ ]*\).*#\1#p' | awk -F/ '{ print ($2 == "" || $2 ~ /\.html$/ ? "(root)" : $2) }'; }
            echo "RESTORED by first segment:"
            grep -a '(restored)' /tmp/pages.txt | seg | sort | uniq -c | sort -rn | head -15
            echo "NOT restored by first segment:"
            grep -av '(restored)' /tmp/pages.txt | seg | sort | uniq -c | sort -rn | head -20
            echo "--- counts ---"
            echo "cached:   $(grep -ac '(cached)' /tmp/build.log)"
            echo "restored: $(grep -ac '(restored)' /tmp/build.log)"
            echo "emitted:  $(grep -acE '^ *(├─|└─)' /tmp/build.log)"
            echo "html:     $(find $d -name '*.html' | wc -l)"
            echo "_astro:   $(find $d/_astro -type f | wc -l)"
            echo "css:      $(find $d/_astro -name '*.css' | wc -l)"
            echo "js:       $(find $d/_astro -name '*.js' | wc -l)"
          } > /tmp/profile.txt
        env:
          NO_IMAGE_OPTIM: ${{ startsWith(github.repository, 'kestra-io/') && github.ref != 'refs/heads/main' && 'true' || 'false' }}
          PREVIEW: ${{ startsWith(github.repository, 'kestra-io/') && github.ref != 'refs/heads/main' && 'true' || 'false' }}

      # A page restored from a stale incremental cache links CSS/JS hashes this
      # build no longer emits. Catch it here rather than as a 404 in production.
      - name: Check for dangling /_astro/ links
        run: node scripts/check-dist-asset-links.mjs dist

      - name: Deploy Worker
        id: prod-deploy
        if: ${{ startsWith(github.repository, 'kestra-io/') && github.ref == 'refs/heads/main' }}
        uses: cloudflare/wrangler-action@v3.14.1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_ACCOUNT_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy

      - name: Deploy Preview Worker
        id: preview-deploy
        if: ${{ startsWith(github.repository, 'kestra-io/') && github.ref != 'refs/heads/main' }}
        uses: cloudflare/wrangler-action@v3.14.1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_ACCOUNT_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: versions upload --preview-alias="${{ needs.truncate-url-slug.outputs.slug }}"

      - name: Add comment to PR
        if: ${{ startsWith(github.repository, 'kestra-io/')  && github.event_name == 'pull_request'}}
        uses: kestra-io/actions/actions/comment-update@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          title: "☁️ Cloudflare Worker Preview Deployed!"
          template: |
            🔗 [${{ needs.truncate-url-slug.outputs.url }}](${{ needs.truncate-url-slug.outputs.url }})
            🔗 [${{ steps.preview-deploy.outputs.deployment-url }}](${{ steps.preview-deploy.outputs.deployment-url }})

      # TEMPORARY (test branch only): last step, so the profile sits at the end
      # of the job log where the API tail can reach it.
      - name: Build profile
        if: always()
        run: |
          # The logs API caps its response at ~7KB from the end of the job, and
          # post-job cleanup eats most of it, so print only the compact part.
          # Full profile is in the Build step's own output. (run 2 of the scope experiment)
          grep -a 'cachekey-probe' /tmp/build.log | sort -u
          sed -n '/--- counts ---/,$p' /tmp/profile.txt
          # Last line of the job: the tail-limited logs API always reaches this.
          echo "KEY restored=$(grep -ac '(restored)' /tmp/build.log) pages=$(grep -ac -e '├─' -e '└─' /tmp/build.log)"
