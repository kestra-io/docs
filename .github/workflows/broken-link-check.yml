on:
  schedule:
    - cron: "0 6 * * 1" # Every Monday morning, Paris timezone
  workflow_dispatch: {}

name: Broken Link Check
jobs:
  check:
    name: Broken Link Check
    runs-on: ubuntu-latest
    steps:
      - name: Broken Link Check
        uses: technote-space/broken-link-checker-action@v2
